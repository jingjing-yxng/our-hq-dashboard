# our-hq-dashboard

Encrypted build artifacts for a private dashboard. The `index.html` here is
AES-256-encrypted ciphertext (staticrypt) generated automatically from a
private repository — there is no readable data in this repo.
